<%= form_with(model: @campaign, local: true) do |form| %>
    <div class="row page-layout page-layout-edit">
        <% if @campaign.errors.any? %>
            <div id="error_explanation">
                <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this campaign from being saved:</h2>

                <ul>
                <% @campaign.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
                </ul>
            </div>
        <% end %>

        <section class="left-col col-md-3 ml-auto">
            <div class="card page-main campaign-main card-motif-blue">
                <div class="card-header">Campaign
                    <div class="card-actions">
                        <%= link_to 'Cancel', @campaign, class: 'cancel-link' %>
                        <%= form.submit 'Save', class: ['btn', 'btn-sm', 'save-btn', 'text-light'] %>
                        <%= link_to 'Delete', @campaign, method: :delete, data: { confirm: 'Are you sure?' }, class: ['btn', 'btn-red', 'btn-sm', 'delete-btn'] %>
                    </div>
                </div>
                <div class="card-body">
                    <div class="form-group">
                        <%= form.text_field :name, class: 'form-control' %><br/>
                        <%= form.text_area :description, oninput:'this.style.height = "";this.style.height = this.scrollHeight + "px"', class: 'form-control' %>                    
                    </div>
                </div>
            </div>
            <%= render 'shared/related_left', referrer_id: @campaign.id, referrer_type: 'Campaign', campaigns: @campaigns %>
        </section>

        <section class="center-col col-md-6">
            <div class="card">
                <div class="card-header">Notes</div>
                <div class="card-body">
                    <div class="form-group">
                        <%= form.text_area :content, oninput:'this.style.height = "";this.style.height = this.scrollHeight + "px"', class: 'form-control' %>
                    </div>
                
                    <% unless @notes.empty? %>
                        <hr>
                        <div class="notes-container">
                        <%= render 'shared/list', card_type: 'full', objects: @notes, referrer_id: @campaign.id, referrer_type: 'Campaign' %>      
                        </div>
                    <% end %>
                </div>
            </div>
        </section>

        <section class="right-col col-md-3 mr-auto">
            <%= render 'shared/related_right', referrer_id: @campaign.id, referrer_type: 'Campaign', locations: @locations, quests: @quests %>
        </section>
    </div>
<% end %>

