<% if related[:locations] != nil && !related[:locations].empty? %>
    <%= render 'shared/related_list_card', type_class: Location, objects: related[:locations], path: locations_path, new_path: new_location_path,
                                           referrer_id: referrer_id, referrer_type: referrer_type, show_unlink: show_unlink %>
<% end %>

<% if related[:quests] != nil && !related[:quests].empty? %>
    <%= render 'shared/related_list_card', type_class: Quest, objects: related[:quests], path: quests_path, new_path: new_quest_path,
                                           referrer_id: referrer_id, referrer_type: referrer_type, show_unlink: show_unlink %>
<% end %>

<% if related[:encounters] != nil && !related[:encounters].empty? %>
    <%= render 'shared/related_list_card', type_class: Encounter, objects: related[:encounters], path: encounters_path, new_path: new_encounter_path,
                                           referrer_id: referrer_id, referrer_type: referrer_type, show_unlink: show_unlink %>
<% end %>
